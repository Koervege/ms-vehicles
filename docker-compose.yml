version: '3.8'
services:
  db:
    image: postgres:latest
    container_name: vehicles-db
    environment:
      POSTGRES_DB: vehicles_db
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
